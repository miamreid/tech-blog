{{#each comments as |comment|}}

{{!-- 
<div class="comments">

    <div class="comment-meta">
        <p>Posted by: {{user.username}}</p>
    </div>
    <div class="comment-text">
        <p>{{comment.comment}}</p>
    </div>

</div> --}}

<div class="card comments" style="margin-bottom: 20px; margin-top: 20px;" >
    <div class="card-body comment-text">
        <h5 class="card-title">{{comment.comment}}</h5>
        <h6 class="card-subtitle mb-2 text-body-secondary">User: {{user.username}} on {{createDate}}</h6>
    </div>
</div>
{{/each}}