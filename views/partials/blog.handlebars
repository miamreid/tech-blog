<div class="card">
    <div class="card-title">
        <h2><a href="/blog/{{{id}}}"></a>{{{blog.title}}}</a></h2>
        <p>Posted by {{{user.username}}}</p>
    </div>
    <div class="card-content">
        <p>{{{blog.content}}}</p>
    </div>
</div>